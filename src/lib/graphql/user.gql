query GetUser {
  user {
    id
    photo
    email
  }
}

mutation Register($input: RegisterInput!) {
  register(input: $input) {
    message
  }
}

mutation Login($input: LoginInput!) {
  login(input: $input) {
    token
  }
}

query RefreshAuth {
  refreshAuth {
    token
  }
}

query Logout {
  logout {
    message
  }
}
